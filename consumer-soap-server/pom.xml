<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.servicehealtherx</groupId>
        <artifactId>basis-consumer-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>consumer-soap-server</artifactId>
    <name>Basis Consumer :: Consumer SOAP Server</name>
    <description>Quarkus application exposing the Basis Consumer SOAP interface (api-telematik/consumer); integrates all internal services via CDI; embeds Hawtio</description>

    <dependencies>
        <!-- Internal services -->
        <dependency>
            <groupId>de.servicehealtherx</groupId>
            <artifactId>crypto-services-lib</artifactId>
        </dependency>
        <dependency>
            <groupId>de.servicehealtherx</groupId>
            <artifactId>quarkus-sicct-extension</artifactId>
        </dependency>
        <dependency>
            <groupId>de.servicehealtherx</groupId>
            <artifactId>quarkus-ldap-proxy-server-extension</artifactId>
        </dependency>

        <!-- Quarkus application core -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-health</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-config-yaml</artifactId>
        </dependency>

        <!-- CXF SOAP server — Basis Consumer interface -->
        <dependency>
            <groupId>io.quarkiverse.cxf</groupId>
            <artifactId>quarkus-cxf</artifactId>
        </dependency>

        <!-- JPA + Derby for AppConfigProperty and CardTerminal -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-orm-panache</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jdbc-derby</artifactId>
        </dependency>

        <!-- Hawtio for JMX management UI -->
        <dependency>
            <groupId>io.hawt</groupId>
            <artifactId>hawtio-quarkus</artifactId>
            <version>4.0.0</version>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkiverse.cxf</groupId>
            <artifactId>quarkus-cxf-client</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- wsdl2java for api-telematik consumer WSDL -->
            <plugin>
                <groupId>io.quarkiverse.cxf</groupId>
                <artifactId>quarkus-cxf-maven-plugin</artifactId>
                <version>${quarkus-cxf.version}</version>
                <executions>
                    <execution>
                        <id>generate-consumer-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>wsdl2java</goal>
                        </goals>
                        <configuration>
                            <wsdlRoot>${project.basedir}/../api-telematik/consumer</wsdlRoot>
                            <includes>
                                <include>**/*.wsdl</include>
                            </includes>
                            <outputDirectory>${project.build.directory}/generated-sources/cxf-consumer</outputDirectory>
                            <packageName>de.servicehealtherx.consumer.soap</packageName>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
