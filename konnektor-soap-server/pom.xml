<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.servicehealtherx</groupId>
        <artifactId>basis-consumer-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>konnektor-soap-server</artifactId>
    <name>Basis Consumer :: Konnektor SOAP Server</name>
    <description>Quarkus application exposing Konnektor SOAP compatibility interface (api-telematik/conn); routes operations to crypto-services-lib</description>

    <dependencies>
        <!-- Internal services -->
        <dependency>
            <groupId>de.servicehealtherx</groupId>
            <artifactId>crypto-services-lib</artifactId>
        </dependency>
        <dependency>
            <groupId>de.servicehealtherx</groupId>
            <artifactId>quarkus-sicct-extension</artifactId>
        </dependency>

        <!-- Quarkus application core -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-health</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-config-yaml</artifactId>
        </dependency>

        <!-- CXF SOAP server — Konnektor compatibility interface -->
        <dependency>
            <groupId>io.quarkiverse.cxf</groupId>
            <artifactId>quarkus-cxf</artifactId>
        </dependency>

        <!-- JPA + Derby -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-orm-panache</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jdbc-derby</artifactId>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkiverse.cxf</groupId>
            <artifactId>quarkus-cxf-client</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- wsdl2java for api-telematik konnektor WSDLs -->
            <plugin>
                <groupId>io.quarkiverse.cxf</groupId>
                <artifactId>quarkus-cxf-maven-plugin</artifactId>
                <version>${quarkus-cxf.version}</version>
                <executions>
                    <execution>
                        <id>generate-konnektor-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>wsdl2java</goal>
                        </goals>
                        <configuration>
                            <wsdlRoot>${project.basedir}/../api-telematik/conn</wsdlRoot>
                            <includes>
                                <include>**/*.wsdl</include>
                            </includes>
                            <outputDirectory>${project.build.directory}/generated-sources/cxf-konnektor</outputDirectory>
                            <packageName>de.servicehealtherx.konnektor.soap</packageName>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
